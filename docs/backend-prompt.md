# 后端生成器 Prompt（若只要后端）

请帮我生成后端代码骨架（Spring Boot + MyBatis-Plus）和数据库 DDL，满足以下业务约定：

## 核心业务约定
1. **订单不可变**：order_item 包含商品快照字段
2. **商品逻辑删除**：product 表支持逻辑删除
3. **购物车 Redis+DB write-through**：同步写策略
4. **下单事务**：包含库存锁定与异步邮件
5. **推荐系统**：热门+离线 item-based CF 缓存到 Redis

## 技术栈要求
- Java 17 + Spring Boot 3.x
- MyBatis-Plus + MySQL 8.0
- Redis（缓存、分布式锁）
- Spring Security + JWT
- Spring Mail、AOP、Scheduling

## 请先输出
1. **设计清单与关键接口**
   - 数据库表结构设计（DDL）
   - 核心业务接口定义
   - 关键配置说明

2. **等待我确认后产出**
   - 完整的代码骨架
   - 配置文件
   - 测试用例

## 关键功能点
- 用户认证授权（三角色）
- 商品管理（CRUD + 逻辑删除）
- 购物车（Redis+DB 同步）
- 下单事务（库存锁定 + 快照）
- 推荐系统（离线计算 + 缓存）
- 行为日志（AOP 采集）
- 数据分析（聚合 + 缓存）

## 验收标准
- 能完成完整下单流程
- 订单快照字段正确保存
- 购物车数据一致性
- 推荐接口正常工作
- 行为日志正常记录

**请先输出设计清单与关键接口，然后等待我确认后产出代码与配置文件。**