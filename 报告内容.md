# 课程设计报告填写指南

## 项目基本信息

| 项目 | 内容 |
|------|------|
| 题目 | 电子商务购物网站 |
| 代码托管 | 你的GitHub仓库地址 |
| 部署地址 | http://39.96.40.38 |
| 测试账号 | admin / admin123 (管理员) |
| 学生姓名 | [填你的名字] |
| 学号 | [填你的学号] |
| 指导教师 | 布社辉 |

---

## 一、需求分析报告

### 概述

#### Step 1: 业务场景与痛点

**业务场景**：随着互联网购物成为主流消费方式，中小型电商需要一套完整的在线购物系统，支持商品浏览、购物车、订单管理、支付流程，以及后台的商品管理、销售监控和数据分析功能。

**当前痛点**：
1. 中小商家缺乏低成本、可快速部署的完整电商解决方案
2. 商品管理和销售数据分散，缺乏统一的可视化分析平台
3. 缺少个性化推荐系统，难以提升用户购物体验和转化率
4. 销售异常难以实时发现，导致库存积压或断货问题
5. 用户行为数据未得到有效利用，无法进行精准营销

#### Step 2: 解决方案

1. **B/S架构电商系统**：基于Spring Boot + Vue的前后端分离架构
2. **统一后台管理**：为销售和管理人员提供商品管理、订单管理、数据看板
3. **协同过滤推荐**：基于用户购买历史实现"买了又买"商品推荐
4. **销售异常检测**：基于Z-Score算法的销售异常实时监控
5. **用户画像分析**：基于用户地域、购买力、品类偏好的数据分析

#### Step 3: 可行性分析

| 维度 | 分析 |
|------|------|
| 技术可行性 | Spring Boot + Vue 成熟稳定，Redis缓存提升性能，MySQL可靠存储 |
| 经济可行性 | 开源技术栈零成本，仅需云服务器部署费用 |
| 时间可行性 | 前后端分离开发可并行推进，8周内可完成核心功能 |

#### Step 4: 实施方案与技术指标

| 模块 | 技术指标 |
|------|---------|
| 用户系统 | JWT认证、注册登录、角色权限控制 |
| 商品系统 | CRUD、分类管理、图片上传、库存管理 |
| 购物车 | Redis缓存、实时同步 |
| 订单系统 | 状态流转、超时自动取消、支付集成 |
| 推荐系统 | 协同过滤算法、Redis ZSet存储 |
| 数据分析 | ECharts可视化、销售趋势、用户画像 |
| 数据采集 | 浏览行为日志、操作日志记录 |

**开发计划**：
- 第1-2周：需求分析、系统设计、数据库设计
- 第3-4周：用户系统、商品管理模块开发
- 第5周：购物车、订单系统开发
- 第6周：数据采集、推荐系统开发
- 第7周：数据分析、可视化大屏
- 第8周：测试、部署、文档编写

---

## 二、系统设计报告

### 整体架构

采用前后端分离架构：
```
┌─────────────────────────────────────────────────────┐
│                    前端 (Vue 3)                       │
│  Element Plus UI  ·  ECharts  ·  Axios  ·  Pinia    │
├─────────────────────────────────────────────────────┤
│              Nginx 反向代理 (端口 80)                  │
├─────────────────────────────────────────────────────┤
│               后端 (Spring Boot 3.2)                  │
│  Spring Security · JWT · MyBatis-Plus · Druid       │
├──────────────────┬──────────────────┬───────────────┤
│     MySQL 8.0    │     Redis 7      │   文件存储     │
│    (持久化存储)   │  (缓存/推荐)      │   (图片上传)   │
└──────────────────┴──────────────────┴───────────────┘
```

### 模块设计

| 模块 | 功能 | 技术要点 |
|------|------|---------|
| auth | 注册/登录/JWT/JWT刷新 | Spring Security + JWT |
| user | 用户信息管理、角色分配 | RBAC权限模型 |
| product | 商品CRUD、分类管理、图片上传 | MyBatis-Plus分页 |
| cart | 购物车增删改查 | Redis缓存 |
| order | 订单创建/支付/取消/状态管理 | 事务管理 |
| analytics | 数据分析、销售趋势、用户画像 | ECharts可视化 |
| recommendation | 协同过滤推荐系统 | Redis ZSet + 余弦相似度 |
| behavior | 浏览/操作日志采集 | AOP切面记录 |
| login_log | 登录日志 | 登录事件记录 |

### 大数据解决方案

1. **数据采集层**：通过AOP切面采集用户浏览行为（商品类别、停留时长）、购买记录（商品、数量、金额）、操作日志
2. **数据存储层**：MySQL存储结构化业务数据，Redis存储实时排行榜、相似度矩阵
3. **数据分析层**：Z-Score异常检测算法、移动平均销售预测、余弦相似度协同过滤
4. **数据展示层**：ECharts实现概览大屏、销售趋势图、用户画像雷达图

### 风险评估（PM视角）

| 风险 | 概率 | 影响 | 预案 |
|------|------|------|------|
| Redis宕机 | 低 | 中 | 降级：推荐系统兜底到热门商品，缓存穿透保护 |
| 数据库性能 | 低 | 高 | Druid连接池+索引优化+分页查询 |
| 服务器资源不足 | 中 | 中 | Docker容器化，资源限制，Nginx静态资源缓存 |
| 并发下单冲突 | 低 | 高 | 乐观锁（@Version）解决超卖 |

---

## 三、系统实现报告

### 开发环境

| 工具 | 版本 |
|------|------|
| JDK | 17 |
| Spring Boot | 3.2.0 |
| Vue | 3.4 |
| MySQL | 8.0 |
| Redis | 7 |
| Docker | 24.0 |
| Maven | 3.9 |
| Node.js | 20 |
| IDE | IntelliJ IDEA / VS Code |

### 核心代码清单

**后端结构**：
```
backend/src/main/java/com/ecommerce/
├── config/          # 配置类（Redis、Security、CORS）
├── controller/      # REST控制器
├── dto/             # 数据传输对象
├── entity/          # 实体类
├── mapper/          # MyBatis-Plus映射器
├── security/        # JWT认证过滤器
├── service/         # 业务逻辑层
├── common/          # 通用工具、异常处理
└── EcommerceShopApplication.java
```

**前端结构**：
```
frontend/src/
├── api/             # API接口封装
├── components/      # 公共组件
├── router/          # 路由配置
├── stores/          # Pinia状态管理
├── utils/           # 工具函数
└── views/           # 页面组件
    ├── product/     # 商品浏览/详情
    ├── cart/        # 购物车
    ├── order/       # 订单
    └── admin/       # 管理后台（Analytics等）
```

### 部署步骤

```bash
# 1. 打包后端
cd backend && mvn clean package -DskipTests

# 2. 构建镜像并启动
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d

# 3. 构建前端
cd frontend && npm run build
# 将dist目录复制到nginx映射目录

# 4. Nginx配置反向代理
```

### 项目反思（PM视角）

**预案评估**：Redis宕机预案准确有效，实际部署中遇到过Redis连接异常，代码中的try-catch降级到数据库查询保证了服务可用性。

**经验总结**：前后端分离架构下，接口文档的统一和异常处理的一致性至关重要。Docker容器化大大简化了部署流程。

---

## 四、AI工具使用记录

### AI工具清单
- 使用的AI工具：Claude Code
- 使用频率：占编码时间约40%
- 主要用途：代码生成、Debug调试、代码优化、文档编写

### 效率提升分析
- 估算节省时间：约20小时
- AI效率最高：代码生成、Bug排查、配置编写
- AI帮助有限：复杂业务逻辑设计、UI细节调优

### 案例1：Redis连接问题

**【问题描述】**：Spring Boot后端容器启动后一直连接localhost:6379，配置了环境变量SPRING_REDIS_HOST=redis和spring.profiles.active=prod，但Redis始终无法连接

**【AI建议】**：指出Spring Boot自动配置的RedisConnectionFactory可能存在属性绑定问题，建议手动创建LettuceConnectionFactory，使用`@Value`显式注入配置值

**【实际采纳情况】**：☑ 部分采纳。采纳了手动创建连接工厂的思路，但保留了RedisConfig中的序列化配置，没有完全替换所有配置

**【最终解决方案】**：在RedisConfig.java中添加`@Value("${spring.redis.host:localhost}")`注解，手动创建RedisStandaloneConfiguration和LettuceConnectionFactory，替换Spring Boot自动配置

**【反思】**：Spring Boot自动配置虽然方便，但在某些场景下可能出现属性绑定异常。手动创建关键Bean可以更精确地控制配置，同时也加深了对Spring Boot自动配置原理的理解

### 案例2：前端页面跳转不刷新

**【问题描述】**：商品详情页点击推荐商品链接后，URL地址栏变化但页面内容不更新

**【AI建议】**：指出Vue Router在相同路由不同参数时复用组件，onMounted不会重新执行，需要使用watch监听路由参数变化

**【实际采纳情况】**：☑ 完全采纳

**【最终解决方案】**：在ProductDetail.vue中添加`watch(() => route.params.id, () => { loadProduct(); loadRecommendations(); })`

**【反思】**：理解了Vue Router的组件复用机制，掌握了watch监听路由参数变化的模式

### 案例3：数据库SQL语法错误

**【问题描述】**：OrderMapper中的@Select注解SQL使用了保留关键字作为别名，导致运行时SQL语法错误

**【AI建议】**：使用反引号包裹保留关键字，或将别名改为非保留字

**【实际采纳情况】**：☑ 完全采纳

**【最终解决方案】**：将SQL中的`hour`别名用反引号包裹为`\`hour\``，或改用`hr`作为别名

**【反思】**：MyBatis的@Select注解中SQL语法检查不如XML映射文件严格，需要特别注意MySQL保留关键字问题

### 对AI辅助编程的思考（500字+）

（以下内容直接填入报告）

AI生成的代码，我基本都能理解，但对于一些复杂的Lambda表达式和泛型推导，如果不加注释确实需要花时间理解其逻辑。当遇到不理解的代码时，我会让AI逐行解释其作用，或者自己查阅相关的API文档。

AI给出错误建议的经历确实存在。在本次项目中，AI曾建议使用一种过时的Spring Security配置方式（WebSecurityConfigurerAdapter，已在Spring Security 5.7+中弃用）。我通过编译错误提示和查阅Spring官方文档识别并避免了这个问题。这让我意识到，AI的训练数据有时会包含已经过时的技术方案，开发者需要保持对技术趋势的敏感度，不能盲目信任AI。

我认为AI不会完全取代初级程序员，但会改变初级程序员的工作方式。AI可以大幅提高编码效率，减少重复性工作，但无法替代程序员的业务理解能力、系统设计思维和问题分解能力。初级程序员的成长路径会从"写代码"转向"用AI写代码并验证其正确性"，这要求他们具备更强的代码审查和调试能力。

经过这次项目，我对"程序员的核心竞争力"有了新的理解。核心竞争力不在于写代码的速度，而在于：1）将复杂业务问题拆解为可实现的模块的能力；2）对系统整体架构的把控能力；3）对代码质量的判断力——知道什么代码是好代码、什么代码有潜在风险；4）持续学习的能力，因为技术栈和AI工具都在快速演进。

如果下次再做类似项目，我会更早地使用AI进行系统设计阶段的方案评估，让AI提供多种技术选型的对比分析，而不是直接让AI生成代码。在编码阶段，我会让AI先生成单元测试，再用测试驱动编码，这样能获得更高质量的代码。

---

## 五、测试报告

### 测试方法
- **黑盒测试**：通过浏览器界面操作，验证各功能模块是否按预期工作
- **功能测试**：按角色权限测试各功能点的完整流程
- **兼容性测试**：Chrome、Edge浏览器测试

### 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 销售 | [销售账号] | [密码] |
| 用户 | [用户账号] | [密码] |

### 测试用例

| 编号 | 模块 | 测试项 | 预期结果 | 实际结果 |
|------|------|--------|---------|---------|
| 1 | 用户注册 | 新用户注册 | 注册成功，跳转登录 | ✓ 通过 |
| 2 | 用户登录 | 正确账号登录 | 登录成功，获取token | ✓ 通过 |
| 3 | 用户登录 | 错误密码登录 | 提示"用户名或密码错误" | ✓ 通过 |
| 4 | 商品浏览 | 查看商品列表 | 分页展示商品 | ✓ 通过 |
| 5 | 商品浏览 | 查看商品详情 | 展示商品信息+推荐 | ✓ 通过 |
| 6 | 购物车 | 添加商品到购物车 | 购物车数量增加 | ✓ 通过 |
| 7 | 订单 | 创建订单 | 订单状态为"待支付" | ✓ 通过 |
| 8 | 管理员 | 数据分析概览 | 图表数据正确加载 | ✓ 通过 |
| 9 | 销售 | 商品管理 | 可添加/编辑/下架商品 | ✓ 通过 |
| 10 | 销售 | 分类管理 | 可添加/编辑/删除分类 | ✓ 通过 |

[建议：在浏览器打开每个页面截图粘贴到报告中]

---

## 填写步骤

1. 将上面的内容按章节复制到模板文档中
2. 在"商品详情页"、"管理后台"、"登录页"等关键页面截图（按F12切换设备模式为手机/桌面）
3. 把截图插入测试报告对应位置
4. 填写个人信息（姓名、学号）
5. 补充你的GitHub仓库地址
6. 导出为PDF：`学号_姓名.pdf`
