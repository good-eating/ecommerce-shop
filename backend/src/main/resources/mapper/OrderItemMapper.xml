<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.OrderItemMapper">

    <insert id="batchInsert">
        INSERT INTO order_item (order_id, product_id, product_name, sku, price_at_purchase, quantity, subtotal, snapshot_json, created_at)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.sku}, #{item.priceAtPurchase}, #{item.quantity}, #{item.subtotal}, #{item.snapshotJson}, NOW())
        </foreach>
    </insert>

</mapper>